# Supabase project settings (Dashboard -> Settings -> API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Reserved for a future server-side admin client (not used in Plan 1 runtime code)
SUPABASE_SERVICE_ROLE_KEY=

# Direct Postgres connection string, used ONLY by scripts/*.mjs (migrations,
# rls-check, pins-check) and e2e/seed.ts -- NOT by the app runtime.
# Format: postgresql://postgres:<db-password>@db.<project-ref>.supabase.co:5432/postgres
SUPABASE_DB_URL=
